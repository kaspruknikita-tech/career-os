# Career OS

Карьерная система для Claude Code. Одна база твоего опыта, шесть скиллов, которые её читают.

Резюме под вакансию, сопроводительное, подготовка к интервью, поиск каналов отклика и ежедневный тренажёр устных ответов — всё берёт факты из одного места. Поэтому резюме, письмо и ответ на собеседовании не расходятся между собой.

## Зачем это, если есть чат

Обычный диалог с ИИ про резюме выглядит так: пересказал свой опыт, получил текст, через неделю пересказал заново. Каждый раз с нуля, каждый раз чуть другими словами, и на интервью выясняется, что в резюме написано одно, а рассказываешь ты другое.

Здесь опыт записан один раз — блоками, с метриками и с пометками, что проверяемо, а что нет. Дальше скиллы собирают из блоков то, что нужно под конкретную вакансию, и никогда не выдумывают того, чего в базе нет.

## Установка

Как плагин:

```
/plugin marketplace add kaspruknikita-tech/career-os
/plugin install career-os
```

Вручную:

```
git clone https://github.com/kaspruknikita-tech/career-os.git
cp -R career-os/skills/* ~/.claude/skills/
cp career-os/commands/* ~/.claude/commands/
```

При ручной установке мастеру нужен путь к склонированной папке — он спросит.

После установки:

```
/career-init
```

Мастер задаст 12–15 вопросов и развернёт твою базу. Займёт минут двадцать, зато один раз.

## Первый запуск

```
/career-init                      развернуть базу, заполнить ядро
/career import <файл>             загрузить старое резюме или перформанс-ревью
вот вакансия [текст], оцени фит   первая проверка, что всё работает
```

Дальше система наполняется сама, по ходу откликов: каждая вакансия оставляет след в пробелах и рыночных сигналах.

## Что умеет

| Команда или запрос | Скилл | Что делает |
|---|---|---|
| «оцени фит», «адаптируй резюме» | `resume-tailor` | ATS-скоринг вакансии, сборка резюме из блоков под её требования |
| «напиши сопроводительное» | `cover-letter` | Письмо на 120–180 слов, метрики только из базы |
| «подготовь к интервью» | `interview-prep` | Вероятные вопросы с рынка, разбор кейсов, питчи |
| «куда откликаться» | `channel-finder` | Рекрутеры, почты, LinkedIn, телеграм, каналы отклика |
| `/career` | `career-data-keeper` | Единственный, кто пишет в базу: новые блоки, закрытие пробелов |
| `/drill` | `daily-drill` | Ежедневная тренировка устных ответов с разбором |
| `/career-help`, `/career-map` | — | Следующий шаг и схема связей скиллов |
| `/career-feedback` | — | Отправить отзыв без личных данных |

## Как устроено

```
<корень>/
├── career-data/     твой опыт: ядро, индекс, блоки, пробелы
├── drill/           состояние тренажёра
├── reference/       методология: форматы, стратегии, источники
└── output/          результаты откликов, создаётся сам
```

Два принципа, на которых всё держится.

**Скиллы не знают твоих фактов.** В них лежит только процесс. Всё личное — в `career-data/`. Поэтому один и тот же скилл работает у любого человека.

**Пишет в базу только один скилл.** `career-data-keeper` — единственный с правом записи. Остальные читают. Иначе через месяц в базе окажется три версии одного кейса, и непонятно, какая настоящая.

## Экономия контекста

База растёт, стоимость запроса — нет. Вместо того чтобы грузить весь опыт целиком, скилл читает ядро и индекс, классифицирует вакансию в архетип и подтягивает только релевантные блоки — обычно 8–14 из всех имеющихся.

Скиллы устроены так же: в начале сессии видно только имя и описание, тело загружается при срабатывании, справочники — только когда понадобятся.

## Где лежат данные

Скилл ищет `career-data/` вверх по дереву от текущей папки. Не нашёл — читает `~/.claude/career-os/config.json`. Не нашёл и там — предлагает `/career-init`.

Это значит: положил базу в проект и работаешь из него — настраивать нечего. Хочешь вызывать откуда угодно — конфиг пропишет мастер. Подробности в `docs/resolver.md`.

## Правила, зашитые в систему

- Метрики только из ядра. Не изобретать, не округлять в свою пользу, не переносить между компаниями.
- Пробел признаётся на интервью, но не в сопроводительном: продающий документ одностороннего чтения даёт получателю готовый критерий отсева.
- Блоки неизменяемы. Каждая адаптация пишется в `output/` вместе с changelog и никогда не становится новым источником истины.
- Контакт без источника не записывается. Правдоподобный ник, собранный из логики адреса, хуже отсутствия контакта.
- У каждого скилла есть проверка перед выдачей — единственная точка, через которую проходят все маршруты сборки.

## Проверка и отзыв

Пробуешь одним из первых — пройди чек-лист из [TESTING.md](TESTING.md): около часа, старое резюме и одна живая вакансия.

Отзыв — одной командой в конце сессии:

```
/career-feedback
```

Сама соберёт, что происходило, задаст три вопроса, уберёт всё личное и после твоего «да» создаст issue. Без Claude — [форма на четыре поля](https://github.com/kaspruknikita-tech/career-os/issues/new?template=feedback.yml).

Визуальная версия онбординга со схемой и интерактивным чек-листом — [kaspruknikita-tech.github.io/career-os/onboarding.html](https://kaspruknikita-tech.github.io/career-os/onboarding.html).

## Лицензия

MIT.
