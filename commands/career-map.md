---
description: Схема Career OS — какие скиллы есть, кто что читает и пишет, в каком порядке их применять
---

Объясни пользователю устройство Career OS. Не выполняй никакой работы по резюме или вакансиям — только объясни. Если система ещё не развёрнута, скажи об этом и предложи `/career-init`.

Выведи следующее, адаптировав под то, что реально есть у пользователя (проверь, существует ли корень системы и насколько заполнена база).

---

## Что где лежит

```
career-data/     твой опыт          пишет только career-data-keeper
drill/           тренажёр           пишет только daily-drill
reference/       методология        правят люди, не скиллы
output/          отклики            пишут resume-tailor, cover-letter, channel-finder
```

## Кто что делает

```
                        ┌─────────────────┐
     документ ─────────▶│career-data-keeper│◀──── интервью по пробелу
     (резюме, ревью)    └────────┬────────┘
                                 │ пишет
                                 ▼
                        ┌─────────────────┐
                        │   career-data/  │  ядро · индекс · блоки · пробелы
                        └────────┬────────┘
                    ┌────────────┼────────────┬─────────────┐
                    │ читают     │            │             │
                    ▼            ▼            ▼             ▼
            ┌──────────────┐ ┌────────┐ ┌──────────┐ ┌───────────┐
   JD ─────▶│resume-tailor │ │ cover- │ │interview-│ │daily-drill│
            │              │ │ letter │ │   prep   │ │           │
            └──────┬───────┘ └───┬────┘ └────┬─────┘ └─────┬─────┘
                   │             │            │             │
                   ▼             ▼            ▼             ▼
              resume.md    cover-letter.md  вопросы    разбор ответа
                   │                                        │
                   │ пробел найден                          │ пробел найден
                   └────────────┬───────────────────────────┘
                                ▼
                          career-data/gaps.md ──▶ обратно в keeper

            ┌───────────────┐
   компания▶│channel-finder │──▶ output/{компания}/channels.md
            └───────────────┘    базу не трогает вообще
```

## Порядок на один отклик

```
1. resume-tailor    оценить фит, собрать резюме      → output/{компания}-{роль}/
2. cover-letter     письмо на ту же вакансию          читает changelog резюме
3. channel-finder   куда отправлять                   рекрутеры, почты, каналы
4. interview-prep   если позвали                      вопросы и разбор кейсов
```

Порядок не случайный: `cover-letter` и `interview-prep` читают `changelog.md`, который пишет `resume-tailor`. Так резюме, письмо и устный ответ говорят одно и то же. Если начать с письма, согласовывать будет нечего.

## Два правила, которые объясняют остальное

**Пишет в базу только `career-data-keeper`.** Остальные читают. Исключение одно: `resume-tailor` дописывает найденные пробелы и рыночные сигналы. Без этого правила через месяц в базе будет три версии одного кейса.

**Блоки опыта неизменяемы.** Каждая адаптация уходит в `output/` вместе с changelog и никогда не становится новым источником истины. Поэтому можно смело резать и переформулировать под вакансию — оригинал цел.

## Обратная связь системы

Каждый отклик оставляет след:

- требование, под которое не нашлось блока → строка в `gaps.md`
- методология из вакансии → строка в `drill/market/signals.md`
- провал на тренировке → `drill/errors.md` и очередь повторов
- реальное интервью через `/drill debrief` → и в пробелы, и в ошибки

Пробелы копятся с счётчиком встреч. Тема, всплывшая трижды, — сигнал про рынок, а не про тебя: либо оформить блоком, либо учить.

---

В конце спроси, что показать подробнее: устройство базы, порядок работы с вакансией или тренажёр.
